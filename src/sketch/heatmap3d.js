import * as firebase from 'firebase';
export default function sketch(p) {

    p.setup = function () {
        p.createCanvas(1500, 1061);
        p.smooth();
    };

    p.draw = function () {
    };
};